import React from 'react';
function MyParent(props) {
    return (
        <div>
          {props.name}  
          {props.id}  
          {props.phone_no}  
    
        </div>
    );
}

export default MyParent;